import 'package:flutter/material.dart';

class Available extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Hospitals' Availability"),
        centerTitle: true,
        backgroundColor: Colors.red[600],
        elevation: 0.0,
      ),


    );
  }
}
